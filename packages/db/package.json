{
  "name": "@olymarket/db",
  "packageManager": "yarn@3.4.1",
  "scripts": {
    "generate": "prisma generate",
    "push": "prisma db push",
    "reset": "prisma migrate reset && prisma db push",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0"
  }
}
